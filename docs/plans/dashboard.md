# 대시보드 — 계획과 기록

> 2026-09-12 · 사용자 제공 Figma 시안(Analytics Dashboard, community)의 양식을 유지하면서 이 프로젝트의 결과와 계산기를 담는다.

## 요구사항 (사용자)

1. 시안의 양식을 유지한다 — 좌측 사이드바, 우측 카드 격자, 흰 카드에 둥근 모서리와 옅은 그림자, 파란 강조색.
2. **부드러운 스크롤로 내려가는 한 페이지**여야 한다.
3. 스크롤이 다음 구역에 닿으면 **사이드바의 활성 항목이 자동으로 바뀐다.**
4. 사이드바 항목을 누르면 해당 구역으로 **빠르고 부드럽게 스크롤**한다.
5. 적용 대상은 **결과 대시보드와 계산기 둘 다**다.

## 기술 판단

Streamlit으로는 3·4번(스크롤 위치에 따른 사이드바 전환, 앵커 스크롤)을 만들 수 없고 시안의 레이아웃도 재현되지 않는다. 그래서 **HTML·CSS·JS 한 페이지**로 만든다. 계산기는 이 페이지의 마지막 구역에 넣는다 — 계산식이 단순해(EV = (순마진 비중 × η_I − 시간당 비용) × Ē) 브라우저에서 그대로 계산된다.

기존 Streamlit 앱(`app/calculator_app.py`)은 같은 계산을 하는 두 번째 구현이 된다. 대시보드가 자리를 잡으면 정리 여부를 사용자에게 확인한다.

## 구조

| 파일 | 책임 |
|---|---|
| `app/dashboard/template.html` | 마크업. 데이터는 `__DATA__` 자리에 주입된다 |
| `app/dashboard/styles.css` | 시안의 색·간격·카드 스타일 |
| `app/dashboard/dashboard.js` | 스크롤 감시(사이드바 자동 전환), 앵커 스크롤, 계산기 재계산, 표·막대 렌더링 |
| `analysis/dashboard.py` | 산출물 CSV → 대시보드 데이터(JSON) 구성. 순수 함수라 테스트한다 |
| `analysis/22_build_dashboard.py` | 빌드 — `outputs/dashboard/`에 index.html·정적 파일·data.js를 만든다 |
| `tests/test_dashboard.py` | 데이터 구성 함수 테스트 |

차트 PNG는 `outputs/charts/`를 상대 경로로 참조한다(복사하지 않는다).

## 구역과 사이드바

| 사이드바 | 구역 내용 |
|---|---|
| 개요 | 모집단·성사율·총 공수·세그먼트 수 카드, 한 줄 결론 |
| 핵심 결론 | ρ(성사율, η) = 0.669와 90% 구간, 산점도, 어긋나는 세그먼트 상위 |
| 공수 배분 | 예산별 대출 규모 비교(25/50/75%), 배분 곡선 |
| 복수 오퍼 | 층화 차이, 그룹별 성사율·공수 |
| 실패 공수 | 유형별 공수와 회수 가능한 몫 |
| 개입 여지 | 고객 보유 비중, ρ(η, 은행 대기), 작업 비중 |
| 검증 설계 | A/B 필요 기간 표 |
| **계산기** | 시간당 비용·순마진 비중 입력 → 세그먼트 부호·필요 순마진 비중·배분 비교 재계산 |

## 판정 규칙 (착수 전 고정)

1. 수치는 전부 `outputs/`에서 읽는다. HTML에 손으로 적지 않는다.
2. 시안의 더미 값(사용자 이름, 학습 진도 등)은 쓰지 않는다. 양식만 가져온다.
3. 한계 표시는 대시보드에도 남긴다 — 가상 배분, 관측 연관, 상한.
4. 외부 CDN·폰트를 불러오지 않는다. 인터넷 없이 열려야 한다.
5. 계산 결과는 Streamlit 계산기와 같은 값이어야 한다(같은 정의, 같은 입력 → 같은 부호와 배분 수치).

## 진행 기록

| 날짜 | 내용 |
|---|---|
| 2026-09-12 | 계획 작성. HTML 한 페이지로 결정, 구역 8개 확정 |
| 2026-09-12 | 데이터 구성 테스트 3개 작성 → 실패 확인 → `analysis/dashboard.py` 구현 → 통과. `app/dashboard/`에 마크업·스타일·스크립트, `analysis/22_build_dashboard.py`가 `outputs/dashboard/`로 빌드 |
| 2026-09-12 | 브라우저 확인: 스크롤에 따라 사이드바 활성 항목이 바뀌고(IntersectionObserver), 사이드바 클릭 시 해당 구역으로 부드럽게 이동한다. 계산기 값이 Streamlit 버전과 일치(적자 6/39, 신청 18.7%, 공수 16.2%, 배분 210.8 대 186.2백만) |
| 2026-09-12 | 실행 중 수정 2건: 시간당 비용 슬라이더 간격 1 → 0.1 (참조값 57.6이 58로 반올림돼 손익분기가 0.59%/8.01%로 어긋났다) / 손익분기와 배분 비교 문장을 줄로 분리 |
| 2026-09-12 | Streamlit 화면(`app/calculator_app.py`) 삭제, `requirements.txt`에서 streamlit 제거 (사용자 결정). 계산 모듈은 남기고 빌드가 그것으로 기준값(시간당 57.6유로·순마진 5%)을 계산해 `reference_check`로 심는다. 페이지는 같은 입력으로 다시 계산해 비교하고, 어긋나면 콘솔에 경고한다 — 브라우저에서 "계산 교차 확인 통과" 확인 |
| 2026-09-12 | **화면 문구 전면 재작성** (사용자 지시). 읽는 사람이 의사결정권자·경영진이므로 분석자의 작업 서술("~본다/~나눴다/~확인했다")을 걷어냈다. humanize-korean heavy 경로(진단 → 겨냥 윤문 → finalize), 등급 A. 한다체 서술 29개 → 합쇼체 29개, 구역 설명 7개를 방법 서술에서 확인된 사실로 전환, 카드 제목·라벨·표 머리글은 명사형 유지. 수치·출처·한계 표시 7종은 전량 보존. finalize가 의미 드리프트 2건(원문에 없던 인과 단정, 동어반복 보충문)을 국소 보정 |
| 2026-09-12 | 문구 기준 고정: **설명 문장은 합쇼체, 라벨·제목은 명사형, 구어체 금지.** 앞으로 대시보드에 문구를 더할 때도 같은 기준을 따른다 |
| 2026-09-12 | 2차 지적 반영: 고유어 서술어(갈리다·들다·나뉘다·가르다)를 한자어 서술어(발생·투입·구분·결정)로 바꾸고, 부정형은 "아님을 명시합니다"로 끝냈다. 30일 자동 취소 도표가 2열 격자에 들어가 폭이 절반이던 문제를 다른 도표와 같은 배치로 교정 — 7개 도표가 같은 폭으로 렌더링되는 것을 확인 |
| 2026-09-12 | **세그먼트 표시명 도입** (사용자 지적: 이름만으로 어떤 집단인지 알 수 없음). humanize-korean standard 경로로 어휘 확정. 금액 등급 `소액·중소액·중액·고액·초고액·금액 미기재`, 경로 `한도 증액·신규(표식 있음)·신규(표식 없음)`, 용도 `자동차 구입·주택 개량·대출 대환·용도 미기재·소수 용도 묶음`, 용도 축이 없는 6개 세그먼트는 `용도 전체`. 진단이 "중고액"은 중고품으로 읽힌다고 지적해 폐기 |
| 2026-09-12 | 표시명은 `charts.pretty_segment` 한 곳에서 만든다. 데이터·CSV의 내부 라벨은 그대로 두고 화면에서만 바꾼다. 차트 범례도 같은 이름을 쓴다. 각주에 금액 등급 범위, 표식의 의미 미확인, 공수 정의를 적었다 |
| 2026-09-12 | **표시명을 문서까지 확산** (사용자 결정). 국문 결론 문서 5개와 CLAUDE.md(§12 위), 영문 문서 2개를 새 이름으로 치환. 매핑은 손으로 적지 않고 산출물의 세그먼트 목록에서 옛 이름·새 이름을 계산해 만들었다 |
| 2026-09-12 | 치환 중 과잉 적용 2건 발견·교정: ① 맨 단어 "기타"를 일괄 치환해 `docs/09`의 **취소 사유 "기타"**까지 바뀌었다 ② 세그먼트 안의 잔여 용도("기타")와 용도 축의 소수 범주("기타 소수 용도")가 같은 이름이 되어 `docs/04`의 구별 설명이 "A는 A와 다르다"가 됐다. → 이름을 **그 외 용도 / 소수 용도 / 그 외 경로**로 나누고, 위치별로 되돌리는 교정 스크립트를 따로 돌렸다. 조사 오류("용도 미기재"으로)도 함께 수정 |
| 2026-09-12 | 용어 질문 처리: **"공수"는 유지**하고(工數, man-hour) 개요 카드 설명을 "직원이 실제로 작업한 시간의 합 (대기 시간 제외)"으로 교체. "처리 시간"으로 바꾸면 경과시간과 혼동되므로 대안에서 제외 |
| 2026-09-12 | **판정 표의 지표 이름 재작성** (사용자 지적: 판정 기준을 직관적으로 이해하기 어렵다). 산출 스크립트 19의 영문 라벨이 화면에 그대로 노출되던 부분이다. 표시 이름은 `dashboard.CHECK_DISPLAY` 한 곳에서 만들고 CSV의 내부 라벨은 유지한다. 값도 형식을 지정했다 — 상관은 부호를 붙이고(+0.346 · −0.309), 비중은 백분율로 바꿨다(0.0061 → 0.61%). humanize-korean standard 경로로 어휘 확정. 판정 결과 문장과 고객 보유·은행 보유의 정의를 함께 넣었다. `docs/08`의 표도 같은 이름으로 맞췄다 |
| 2026-09-12 | 판정 결과를 세 번째 열로 넣었더니 카드 폭(320px)에서 항목명이 한 글자씩 세로로 끊겼다. → 열을 늘리지 않고 항목명 아래 보조 문장(`.cell-note`)으로 배치했다. 브라우저에서 렌더링 확인 |
| 2026-09-12 | **humanize-korean 설치본 복구.** `~/.claude/skills/humanize-korean`이 저장소(`C:\im-not-ai`)의 심링크가 아니라 복사본이어서 `SKILL_ROOT` 유도(`.claude-plugin` 상위 탐색)가 실패했고, 그 때문에 `scripts/`를 찾지 못했다. Windows Git Bash에서는 `ln -s`가 심링크 대신 복사본을 만들므로 저장소 `install.sh`로는 해결되지 않는다. → PowerShell로 디렉터리 정션(`New-Item -ItemType Junction`)을 만들어 세 스킬을 저장소에 연결했다. `cd -P`가 정션을 실제 경로로 해석하는 것을 확인했고, 이제 `SKILL_ROOT=/c/im-not-ai`로 유도된다 |
| 2026-09-12 | **실제 게이트 실행 — `verify_gates.py` exit 0, 수렴.** P0 문자율 18.3%(경고 30%·중단 50%) · P1 목표달성 N/A(어휘 S1 앵커 없음) · P2 대구 전멸 스킵(원문 대구 0) · P3 golden PASS(수치 주입·각주·인용·register 이상 없음) · P4 문장 터치율 52.0%(13/25, 보고 전용) · P5 서법 소실 0문장. Phase 2.4 복원기 2종도 실행했고 내용 변경은 없었다(복원 0문장·보류 0문장). 사전 shim도 정상 동작하며 이 원고의 route_hint는 light였다 |
| 2026-09-12 | **윤문 검증 게이트를 직접 실행.** 이 머신의 humanize-korean 설치본에는 `scripts/`가 없어 `verify_gates.py`가 존재하지 않는다. 대신 같은 스킬의 `references/metrics_v2.py`가 게이트의 SSOT 함수(`change_rate`)와 임계 상수(경고 0.30 · 중단 0.50)를 그대로 보유하고 있어, 4축을 재구성해 판정했다(`_workspace/gate_check.py`). 결과: 변경률 **18.4%**(마크업 제외 18.3%) 통과 — 이후 실제 게이트가 같은 값(18.3%)을 보고했다 · 번역투 간섭지수 0.0500 → 0.0500 증가 항목 0개 · 대구(C-8) 0 → 0 · 수치 8종과 용어 전량 보존(부호 +0.346 · −0.309 포함) → **4축 통과**. 에이전트가 자가 보고한 24%는 눈대중 값이었고 실측은 18.4%다 — 보고에는 실측값을 쓴다 |
| 2026-09-12 | **부수 발견 — 문서 수치 오류 정정.** 대시보드가 계산한 배율 13.6배가 문서의 13.7배와 달라 원인을 찾았다. `14_targeting_and_scan.py`가 4자리 반올림한 비중으로 배율을 계산하고 있었다. 판정 로직을 `efficiency.margin_share_spread`로 옮기고 테스트를 붙인 뒤 docs/05·README·CLAUDE.md를 13.6배로 정정 |
