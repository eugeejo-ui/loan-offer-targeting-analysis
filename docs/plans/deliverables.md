# 산출물 단계 — 계획과 기록

> 2026-09-12 · 핵심 경로 Phase 0~10 종료 후의 마무리 작업. 발표자료는 범위 밖이다(사용자가 직접 제작).

## 범위

| 산출물 | 상태 | 결정 |
|---|---|---|
| README 국·영 | ✅ 2026-09-12 | [`README.ko.md`](../../README.ko.md) · [`README.md`](../../README.md). 국문은 humanize-korean standard 경로 |
| 차트 7개 | ✅ 2026-09-11 | [`visuals.md`](visuals.md) |
| 계산기(MVP) | 진행 | **새로 만든다** (2026-09-12 사용자 결정). 외부 TCO 엔진을 상속하지 않고 이 저장소 안에서 완결한다 |
| 영문화 | 대기 | **핵심 3개 문서만** (2026-09-12 사용자 결정) — `05_expected_value` · `09_requirements` · `10_options`. 나머지는 국문 유지 |
| GitHub 공개 | 미결 | 공개 시 과거 커밋의 CLAUDE.md·JD 처리 필요 |

## 계산기(MVP)

**목적:** Phase 9의 FR1·FR2·MR1이 정의한 규칙이 실제로 계산되는지 확인하는 최소 도구다. 예측 모델이 아니라 **규칙표 계산기**다. 알 수 없는 두 값(시간당 비용, 순마진 비중)을 사용자가 바꿔 가며 세그먼트의 부호와 순위가 어떻게 움직이는지 본다.

**입력:** `outputs/p5_calculator_input.csv`, `outputs/p5_calculator_meta.json` (Phase 5 산출)

**구조:**

| 파일 | 책임 |
|---|---|
| `analysis/calculator.py` | 계산 로직. Streamlit을 import하지 않는다 — 테스트 가능하게 분리 |
| `app/calculator_app.py` | 화면. 입력 위젯과 표·요약만 담당 |
| `tests/test_calculator.py` | 부호 판정·필요 순마진 비중·배분 비교의 경계 사례 |

**화면 구성:**
1. 입력 — 시간당 비용(유로), 총이자 중 순마진 비중(%)
2. 세그먼트 표 — η 순 정렬, 신청당 기대값과 부호, 필요 최소 순마진 비중
3. 요약 — 음수 세그먼트 수와 그 신청·공수 비중, 부호가 뒤집히는 임계 비중
4. 배분 비교 — 공수 예산을 정하면 효율 순서와 성사율 순서의 기대 대출 규모 차이

**판정 규칙 (착수 전 고정):**
1. 계산은 Phase 3·5의 정의를 그대로 쓴다. 새 지표를 만들지 않는다.
2. 기본값은 참조 인건비 57.6유로/시간이고, 그 근거(Eurostat, 네덜란드 금융·보험업 2016)를 화면에 적는다.
3. 세그먼트 순위는 두 입력과 무관하다는 점을 화면에서 보여준다 (P1).
4. 예측·추천 문구를 쓰지 않는다. 계산기는 "이 가정에서 부호가 어떻게 되는가"만 답한다.
5. 데이터가 없으면(캐시 미생성) 안내 문구를 띄우고 종료한다. 예외로 죽지 않는다.

## 영문화

- 대상 3개. 번역이 아니라 영어 독자가 바로 읽는 문서로 쓴다. 용어는 영문 README 기준을 따른다.
- 파일명은 `docs/05_expected_value.en.md` 형식. 국문 원본 상단에 언어 전환 링크를 단다.
- 수치·표 구조·한계 표시는 국문과 1:1로 맞춘다.

## 진행 기록

| 날짜 | 내용 |
|---|---|
| 2026-09-12 | 계획 작성. 계산기는 새로 제작, 영문화는 핵심 3개로 범위 확정 |
| 2026-09-12 | **계산기 완료** — `analysis/calculator.py`(계산) + `app/calculator_app.py`(화면) + 테스트 5개. 테스트 먼저 작성해 실패 확인 후 구현, 전체 68 passed. `requirements.txt`에 streamlit 추가(분석 스크립트는 이 패키지 없이 동작) |
| 2026-09-12 | 기동 확인: `python -m streamlit run app/calculator_app.py`로 띄워 브라우저로 검증. 기본 가정(57.6유로/시간·순마진 5%)에서 적자 6/39, 신청 18.7%·공수 16.2%. 배분 비교 210.8 대 186.2백만 유로(차이 13.2%)로 Phase 5 산출물과 일치. 손익분기 0.58%~7.95%, 배율 13.6배 |
| 2026-09-12 | 실행 중 수정: Streamlit 1.63의 `use_container_width` 사용 중단 예고 → `width="stretch"`로 교체 |
| 2026-09-12 | **영문화 완료** — `05_expected_value.en.md`·`09_requirements.en.md`·`10_options.en.md`. 국문 원본 3개 상단에 언어 전환 링크를 달았고, 영문 README의 링크도 영문판을 가리키게 바꿨다. 세그먼트 라벨은 데이터에서 국문이므로 05 문서 머리에 대응표를 적었다(금액 미기재 = amount not stated 등). 수치·표 구조·한계 표시는 국문과 1:1 |
